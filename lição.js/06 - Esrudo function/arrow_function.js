const doubleVelocity = (velocity) => { // assim é uma sintax de função anonima, 
    return velocity * 2
}

console.log(doubleVelocity(60))

const velocidade = (velocity) => velocity * 2 // assim é a mesma coisa que esta encima

console.log(velocidade(60))